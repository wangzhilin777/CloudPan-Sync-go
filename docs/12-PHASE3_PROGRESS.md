# 三期进度记录（Phase 3 Progress）

## 当前已完成

- 已完成控制台第一轮中文化收口，优先修正普通用户首屏能直接看到的标题、按钮、表单标签和提示语。
- 已把授权页、任务预览页中的关键英文标签收口为中文优先表达，例如“网盘源、风控档位、执行模式、风控覆盖、条目”。
- 已把运行按钮、暂停、继续、重试、能力概览、推荐风控等高频操作文案统一为中文。
- 已补充服务启动阶段的中文提示，启动后会输出本地访问地址、局域网提示、数据目录和默认密码风险提示。
- 已开始同步前端相关测试断言，避免出现“界面已改中文但测试仍卡英文”的脱节状态。
- 已为任务向导补上“目标根目录”输入，预览请求、创建任务、按任务重建向导和复制创建参数都会保留该字段。
- 已打通“目标根目录 + 源端相对路径”的后端映射链路，目标端 metadata 预检查与实际上传已使用映射后的目标路径。
- 已补充后端与静态资源测试，覆盖 `targetRoot` 元数据保存、上传路径映射、结果里的源路径保留，以及页面字段渲染。
- 已新增任务向导内嵌目录浏览器第一版，支持按源/目标授权档案浏览目录、返回上级、刷新目录，并回填源根目录与目标根目录。
- 已为目标目录浏览器补上“在当前目录新建文件夹”入口，用于先建目标目录再回填到任务向导。
- 已为目录浏览器补上“当前目录 / 已选目录”状态标记，降低选完目录后难以确认当前选择的问题。
- 已为目录浏览器补上当前路径面包屑导航，支持按目录层级快速跳回上层路径。
- 已为源目录与目标目录浏览器补上兼容提示和错误引导，在未选网盘源、网盘源不支持目录浏览、缺少稳定目录标识或目录加载失败时，直接提示可验证授权、回到根目录或手动填写路径。
- 已为目录浏览器补上“已选目录自动滚动定位 + 聚焦高亮”，选定目录后会自动滚动到可视区域并短暂强调当前选项，减少长列表里反复查找。
- 已为“选定根目录(JSON)”与“目标根目录”补上手动填写说明，并在目录浏览不受支持时自动改为更直白的降级提示，减少用户卡在路径格式上的情况。
- 已为源目录与目标目录浏览器补上当前层级提示，根目录和非根目录都会直接显示所在层级，减少用户只靠路径字符串判断当前位置的负担。
- 已为目录浏览器补上更清晰的空目录引导：源端空目录会提示可直接使用当前目录，目标端空目录会提示可直接使用当前目录或新建子目录，减少用户停在空列表无从下手的情况。
- 已把 provider 降级态提示改成字段级引导：不支持目录浏览或目录加载失败时，会直接提示去填写“选定根目录(JSON)”或“目标根目录”，减少用户只看到“手动填写路径”却不知道该填哪里的情况。
- 已把目录加载失败提示细化为恢复引导：失败后会直接提示先刷新目录、切回根目录，再失败时改填“选定根目录(JSON)”或“目标根目录”，减少用户只看到错误却不知道怎么恢复的情况。
- 已细化目录操作成功提示：刷新目录、返回上级后会直接显示当前所在源目录或目标目录，减少用户操作成功后还要再靠路径区域确认结果。
- 已细化目录打开成功提示：点击子目录或面包屑后，会直接提示已进入的源目录或目标目录路径，减少用户来回比对路径栏确认是否跳转成功的情况。
- 已细化目录选择结果提示：设为源目录或目标目录后，会直接提示已回填到“选定根目录(JSON)”或“目标根目录”，减少用户选完目录后还要再回头确认字段是否更新的情况。
- 已把目录浏览器“回填结果”区改成更明确的字段摘要，除了显示路径，还会直接显示“已回填到选定根目录(JSON)”或“已回填到目标根目录”，减少用户只靠 toast 判断结果。
- 已细化目录创建结果提示：新建目标目录后会直接提示已进入该目录并回填到“目标根目录”，减少用户再去分别确认“目录是否创建成功”和“目标根目录是否已更新”的来回操作。
- 已把源目录浏览器“使用当前目录 -> 回填选定根目录(JSON)”加入 UI smoke 验收，除了确认 `选定根目录(JSON)` 字段值，还会确认摘要区同步显示“已回填到选定根目录(JSON)：/”，减少源目录回填只改输入框、却漏掉结果提示联动的问题。
- 已把“使用当前目录 -> 回填目标根目录”加入 UI smoke 验收，避免目录选择体验只靠静态文案断言，补上一段真实浏览器交互验证。
- 已把目标目录浏览器“回填结果摘要”加入 UI smoke 联动验收，除了确认 `目标根目录` 字段值，还会确认摘要区同步显示“已回填到目标根目录：/”，减少目录回填只校验输入框、却漏掉界面反馈不同步的问题。
- 已把源目录浏览器真实 smoke 的前置补齐为“先选源授权档案再验收目录回填”，避免测试场景因缺少源授权档案而误判目录选择链路不可用。
- 已优化目标目录创建后的连续操作体验，新建目录后会自动进入该目录并回填为目标目录，减少“创建 -> 刷新 -> 再查找 -> 再选择”的重复步骤。
- 已把任务预览区的推荐原因说明做了首轮中文友好转换，像“多个顶层目录更适合按子树推进”这类英文推荐理由会直接显示为中文，减少小白在创建任务时还要自己翻译建议说明的负担。
- 已把执行模式提示改成“中文解释 + 技术模式名”组合展示，例如“先完整扫描再执行（pre_scan_flat）”和“按目录逐棵推进（leaf_first_lazy）”，减少任务向导里只出现模式 id 让普通用户看不懂的情况。
- 已把目标网盘源洞察卡里的高频英文标签收口为中文，例如“自动补传时段、校准覆盖、校准完成、已覆盖字段、推荐风控档位、恢复预算、预算建议”，减少普通用户在选择目标网盘源时先看到一堆英文说明的阅读负担。
- 已把目标授权档案洞察卡里的 `auth profile riskDefaults` 来源标识收口成“授权档案内置账号默认风控”，并补上中文解释，减少普通用户看到内部实现名词却不知道来源含义的情况。
- 已把目标授权档案洞察卡标题里的授权方式改成“中文解释 + 技术模式名”，例如“手动令牌（manual_token）”，减少普通用户只看到原始授权模式名却不知道是什么意思的情况。
- 已把“已保存授权档案”列表里的原始授权模式名也同步收口成中文解释，例如“手动令牌（manual_token）”，避免详情卡和列表页对同一授权方式出现两套表达。
- 已把“已保存授权档案”列表里的 `Provider / Status / Edit / Validate / Delete / saved / verified / pending` 等直接可见英文收口成中文优先表达，例如“网盘源、状态、编辑、验证授权、删除、已保存（saved）、待校验（pending）、已校验（verified）”，减少普通用户在列表页里中英混杂阅读的负担。
- 已把目标授权档案洞察卡里的 `附加字段 / 命中字段` 收口成更偏用户语言的 `附加配置项 / 已启用字段`，减少普通用户看到实现味过重的字段名却不知道该怎么理解的情况。
- 已把 Provider 卡收口成“默认简版 + 高级信息折叠”形态，首屏默认只展示名称、状态、授权方式、核心能力、推荐风控和授权入口提示，不再一打开就铺满 fallback、冲突策略、风控提示和恢复预算等大段高级信息。
- 已把 Provider 卡里的高频英文标签同步收口成中文表达，例如“兜底策略、冲突策略、风控特征、风控提示、默认风控、账号默认来源、账号默认建议”，减少 Provider 页首屏中英混杂的阅读负担。
- 已为授权档案表单补上随网盘源和授权方式联动的中文授权说明卡，会直接提示当前常见必填项、可留空项，以及 `domainId / driveId / pwdId` 这类需要写进 Extra JSON 的特殊字段示例，减少普通用户面对 Token、Cookie、Extra JSON 时无从下手的情况。
- 已把授权档案创建与校验共用的前端错误入口补上中文错误翻译，像 `missing_access_token / missing_cookie / missing_domain_or_drive_id / missing_pwd_id` 这类高频授权失败场景，现在会直接提示该补哪个字段，而不是只抛后端英文错误。
- 已把状态矩阵、最近结果、最近 probe 和协议族覆盖摘要里的高频英文表头与标签收口成中文，例如“网盘源、协议组、执行模式、扫描模式、重试范围、重试路径、源端删除策略、样本上下文”，减少证据页首屏密集英文缩写带来的阅读负担。
- 已继续把证据页摘要卡里的高频英文指标名收口成中文，例如“任务总数、自动补传任务、等待冷却、已验收协议组、上传成功样本、补传优先动作、验收动作汇总”，让状态矩阵页不再只表头中文、摘要卡仍大段英文。
- 已继续把证据页和状态页里仍直接暴露给普通用户的英文残留收口成中文，例如“优先基础样本、优先上传样本、优先异常样本、优先代表样本、代表样本缺口、恢复优先动作、公平性优先动作、样本上下文、等待原因”，减少状态诊断区仍需用户自行翻译英文摘要的负担。
- 已把网盘源级真实样本验收与默认风控校准里的英文计数标签同步收口成中文，例如“就绪、部分就绪、待补齐、缺基础样本、缺上传样本、缺异常样本、缺代表样本、已覆盖字段、推荐风控”，让证据页在高密度摘要场景下也保持中文优先。
- 已落地 OpenList / Alist 授权引导第一步：授权档案表单顶部新增连接配置区，支持分别填写 OpenList / Alist 地址和访问令牌，并提供“OpenList 优先 / Alist 兜底 / 手动高级模式”三段式入口切换，减少普通用户一上来只能硬填 Token、Cookie、Extra JSON 的困惑。
- 已把 OpenList / Alist 引导配置接入本地持久化，刷新后会保留当前优先入口与已填写地址，并同步更新授权说明卡，让当前是“OpenList 优先”“Alist 兜底”还是“手动高级模式”能直接在表单里看懂。
- 已为授权引导区补上“打开 OpenList / 打开 Alist / 清空引导配置”入口，并保持原有授权档案创建链路可继续使用，确保三期主线三的引导增强不会破坏现有手动授权与 smoke 主流程。
- 已把 OpenList / Alist 授权引导从纯说明推进到“真实检测”第一步：后端新增 `assist/discover` 接口，前端可直接触发 OpenList 或 Alist 连通性检查，并返回当前可见存储列表，减少用户只能凭感觉判断地址和令牌是否可用的情况。
- 已把 OpenList / Alist 发现结果接回授权表单，检测成功后会直接在引导区显示可见存储摘要；如果当前地址可通但没有返回存储，或令牌无效，也会给出更直接的中文反馈，方便用户决定继续补权限还是切回手动模式。
- 已把 OpenList / Alist 发现结果从“纯摘要提示”推进到“可选择回填”：检测成功后会把可见存储渲染成可点击列表，选中后自动回填显示名称，并把 `assistKind / assistBaseUrl / assistStorageId / assistStorageDriver / assistStorageMountPath / assistStorageName / assistStorageStatus` 写入 Extra JSON，方便后续继续补真实授权字段。
- 已把“发现结果点选回填”加入前端静态断言和 UI smoke，避免只显示出存储列表，却没有实际验证“点击后是否真的把授权表单补齐”的脱节情况。
- 已新增桌面模式命令入口 `cmd/cloudpan-sync-desktop`，并补上 `internal/desktop` 启动骨架：桌面模式会把服务绑定到 `127.0.0.1:0` 随机本地端口，再启动控制台面板，避免继续占用固定 `:8080` 端口。
- 已把服务启动链路抽成可复用的监听模式，`App` 现在支持按外部 listener 启动并回传实际控制台地址，为后续接入 WebView/Wails 原生窗口和桌面端优雅退出打下基础。
- 已为桌面模式补上“Chrome/Edge app 模式优先、系统浏览器兜底”的窗口打开策略，让桌面入口在当前阶段就能以独立窗口样式启动，而不是只输出地址让用户自己复制粘贴。
- 已把 Release 工作流扩展到桌面客户端构建矩阵，新增 Windows / Linux / macOS 的 `cloudpan-sync-go-desktop-*` 资产命名，并把桌面包一并纳入 Release 说明和 `SHA256SUMS.txt` 覆盖范围。
- 已补 README 的桌面包下载说明、桌面包与服务端包区别说明，以及 Windows / Linux / macOS 的桌面启动示例，让用户能先判断该下桌面包、服务端包还是 Docker 包。
- 已为桌面模式补上“窗口退出即联动服务退出”的清理链路：桌面入口现在会为 Chrome / Edge app 窗口单独创建临时浏览器 profile，并在独立窗口进程退出后主动取消上下文，带着内置 Go 服务一起收尾，减少桌面窗口关了但后台服务还挂着的情况。
- 已把桌面模式浏览器启动参数补成更独立的软件样式，包括专用 `user-data-dir`、禁用首次运行向导和默认浏览器检查，为后续升级到真正的 WebView / Wails 窗口前先把现有桌面体验做得更稳定。
- 已补三期双语基础设施第一版：首屏 Hero、导航、登录卡、会话状态和登录/退出提示已接入统一翻译字典，支持 `zh-CN / en-US` 切换，并把语言选择持久化到本地存储。
- 已把语言切换加入前端静态断言与 UI smoke，能够在浏览器里真实切换英文首屏文案，再切回中文继续完成主流程，避免双语只停留在静态字典未真正生效。
- 已把双语覆盖扩到“网盘源 / 授权”主流程第一批高频区域：授权档案标题、OpenList / Alist 引导区、字段标签、创建按钮，以及刷新/切换/创建等高频提示已接入统一翻译字典，英文模式下能直接读懂授权页入口。
- 已把授权页英文切换加入 UI smoke，浏览器会在登录后切到英文验证 `Auth Profiles`、`Prefer OpenList`、`Create Auth Profile`，再切回中文继续原有授权与任务主流程，避免授权页只做静态翻译却没验证切换真实生效。
- 已把双语覆盖继续扩到任务向导第一批高频区域：任务预览/创建标题、源/目标网盘源、源/目标授权档案、源/目标目录选择、风控档位、执行模式、源端删除策略、选定根目录、目标根目录、预览结果等静态入口已接入统一翻译字典，英文模式下可直接进入任务向导主流程。
- 已把任务向导中的目标网盘源洞察卡、目标授权档案洞察卡、执行模式推荐卡和预览结果摘要的首批动态标题接入双语词典，减少切到英文后仍出现整块中文说明的情况。
- 已把目录浏览器第一批动态双语补齐到任务向导：根目录、当前层级、空目录提示、目录兼容提示、目录操作按钮、已选状态、手动填写说明和目录回填摘要已能随语言切换联动，减少源目录/目标目录选择环节中英混杂的问题。
- 已把任务向导英文切换加入 UI smoke，浏览器会在进入 `wizard` 页后验证 `Task Preview / Create`、`Source Provider`、`Target Provider`、`Source Directory`、`Use Current Directory`、`Preview Result`，再切回中文继续原有创建任务主流程，避免任务向导只补静态字典却没验证真实切换生效。
- 已把双语覆盖继续扩到任务页与状态页第一批高频区域：任务详情摘要、运行检查点、重试队列标题、下一步处理、运行证据摘要等静态入口与首批动态标题已接入统一翻译字典，英文模式下切到 `tasks / status` 不再只剩向导页可读。
- 已把语言切换的重渲染补到任务预览、任务页、状态页和目录浏览器，切换 `zh-CN / en-US` 后会立即刷新任务摘要、状态摘要、运行检查点与目录提示，避免只切换静态标签、动态区域仍停留旧语言。
- 已把任务页中最容易暴露给普通用户的动态中文残留做第一轮双语化，包括“选定根目录、目标根目录、推荐模式、推荐风控、源端删除策略、重试范围、下一步摘要、后台补传候选、队列拆分”等摘要卡，以及运行检查点和自动补传接管提示。
- 已把状态页运行证据摘要中的首批动态指标做双语化，包括“任务总数、已完成任务、阻塞任务、执行模式、扫描模式、自动补传任务、可直接放行、等待冷却、等待时间窗、等待授权刷新、等待本地恢复、等待人工确认”等高频指标。
- 已把任务页 / 状态页英文切换加入 UI smoke 主流程，浏览器会在任务运行后验证 `Task List`、`Task Details`、`Retry Queue`、`Runtime Checkpoints`，并在状态页验证 `Runtime Evidence Summary`、`Auto-Recovery Pool`、`Protocol Coverage`、`Provider Status Matrix`、`Recent Probe`、`Recent Results`，再切回中文继续后续流程，避免双语只做静态字典未真实验收。
- 已继续把任务页 / 状态页中的目录树、待补传树和删除记录摘要做第一轮双语收口，包括“目录状态、待补传树、最近目录状态、最近待补传树、删除记录摘要、按全部删除记录重建向导、收起全部、展开全部、复制当前路径、仅叶子节点、仅问题节点、清空筛选”等高频入口。
- 已把目录树与待补传树里的首批动态按钮和空状态接入翻译字典，包括“按当前路径重建向导、重试当前路径、后台补传当前路径、复制当前子树、只看父级、只看当前路径、同步到待补传树、同步到目录树、按当前 root 重建向导、同步另一棵树、暂无目录状态、暂无待补传项”等交互文案，减少切到英文后树面板仍大段中文的情况。
- 已补充删除记录摘要区域的双语文案，覆盖“删除记录摘要、用此删除记录重建向导、按全部删除记录重建向导、原因、删除时间、暂无可展开样本、还有 N 条未展开”等提示，避免任务运行后的恢复区在英文模式下继续夹杂中文。
- 已继续把任务页重试队列摘要和筛选结果说明接入双语词典，覆盖“当前显示、可立即重试、阻塞、耗尽、当前没有需要后续重试的队列项、筛选后没有命中的重试队列项、定位待补传树、只看同类队列”等高频提示，减少英文模式下重试区仍夹杂中文统计说明的情况。
- 已把任务处理建议卡的首批动态步骤与按钮接入双语词典，覆盖“刷新授权档案、补回本地回退文件、修复 provider 会话缺口、等待冷却窗口结束、等待自动补传时间窗、等待人工确认、调整重试策略”等引导标题，以及对应的“打开授权面板、只看授权失效队列、定位待补传树、打开任务向导、按当前阻塞打开状态矩阵”等操作按钮。
- 已把部分 blocked action 摘要说明接入双语词典，例如“刷新授权后继续、补回本地文件后继续、等待冷却到指定时间、等待时间窗到指定时间、人工确认后继续、调整重试策略后继续”，让任务详情页里的下一步摘要在英文模式下更连贯。
- 已继续把状态页自动补传最近结果摘要接入双语词典，覆盖“最近预演、最近执行、预演可放行、recovered、最近一次后台补传预演或执行暂无决策明细、预算占用、等待态说明、只看该状态、只看该 lane、采用建议预算、预演该决策、执行该决策、打开样本任务”等高频文案，减少状态页最近结果区在英文模式下仍夹杂中文操作说明的情况。
- 已把验收报告摘要中的首批动态标题接入双语词典，覆盖“报告标题、生成时间、报告备注、自动补传验收、自动补传恢复与公平性摘要、恢复优先动作、恢复优先动作统计、公平性缺口、公平性优先动作、等待原因、当前没有自动补传候选池样本”等高频说明，让状态页报告区在双语切换后更完整。
- 已把真实 Provider Smoke 记录和验收矩阵摘要的首批动态入口接入双语词典，覆盖“暂无真实 provider smoke 记录、当前没有 smoke 记录、显示全部 N 条 smoke 记录、当前显示 N / M 条 smoke 记录、暂无真实样本矩阵、查看 Markdown、下载 Markdown、验收矩阵视图”等高频提示，减少状态页样本区仍残留中文统计提示的情况。
- 已继续把协议族覆盖摘要的深层动态说明接入双语词典，覆盖“已取样、待取样、涉及网盘源、样本上下文”等状态页协议族覆盖摘要，让切到英文后不再只剩标题双语、详情仍固定中文。
- 已继续把 Provider 默认风控校准摘要的深层动态说明接入双语词典，覆盖“默认模板缺口速览、校准缺失字段统计、校准优先动作统计、校准就绪度、优先校准、时间窗来源、时间窗建议、校准缺失、校准覆盖、已覆盖字段、校准样本建议”等高频状态页校准说明。
- 已继续把真实样本验收矩阵的高频操作按钮接入双语词典，覆盖“打开 smoke 样本、打开任务样本、预填 smoke 表单、预填账号默认风控、只看该组记录、只看此类”，让状态页样本补齐操作在双语切换后保持一致。
- 已继续把真实样本摘要卡接入双语词典，覆盖“样本数、成功、上传成功、失败、网盘源、涉及网盘源、样本、优先基础样本、优先上传样本、优先异常样本、优先代表样本、最近 smoke 时间”等高频状态页样本摘要说明，减少样本摘要区继续夹杂英文标签的情况。
- 已继续把真实样本验收矩阵筛选条与矩阵详情接入双语词典，覆盖“全部、已验收、进行中、待补齐、当前筛选为空、smoke 样本、任务样本、就绪度、验收清单、缺口、下一步动作、验收优先动作、验收缺口、验收动作、验收建议、最近 smoke / 覆盖观察”等高频状态页验收提示，让状态页样本矩阵在双语切换后更完整。
- 已继续把授权页与任务向导首屏高频静态表单标签接入双语词典，覆盖“已保存授权档案、支持 validate 与 delete、请求间隔、目录间隔、分页大小、冷却时间、重试次数、最大并发、自动补传开始时段、自动补传结束时段、风险关键词、同步到 JSON、清空覆盖、阈值(MB)、冲突策略、条目(JSON)”等高频入口，减少普通用户进入授权和向导首屏时仍看到未解释英文或未纳入语言切换的表单字段。
- 已继续把状态页自动补传筛选栏、报告区和真实 Provider Smoke 记录区的高频静态标签接入双语词典，覆盖“全部模式、全部策略、全部协议族、全部 provider、全部授权档案、全部失败类型、全部阻塞动作、全部执行状态、本轮上限、模式预算、lane 预算、协议族预算、provider 预算、账号预算、报告标题、报告备注、等待报告数据、全部结果、等待 smoke 记录”等高频入口，减少状态页筛选与录入区域仍中英混杂的问题。
- 已把状态页真实样本说明和待补传树筛选区剩余静态文本补上双语接线，覆盖“按 protocol group 聚合真实样本、真实样本矩阵，合并 smoke 记录与协议族 coverage、筛选记录标题 / provider / note、筛选 protocolGroup、筛选 sampleType / reusePriority、providerKey、protocolGroup、authMode、记录标题、备注、操作清单、仅叶子节点、清空筛选、等待状态数据”等首屏可见提示，减少用户切到英文后仍要混读中文占位文案的情况。
- 已继续把授权页“账号默认风控模板”和状态页“最近重试队列”剩余静态文案补上双语接线，覆盖“请求间隔、目录间隔、分页大小、冷却时间、重试次数、最大并发、自动补传开始/结束时段、风险关键词、筛选路径 / reason / provider status、全部分类、全部状态、等待状态数据”等高频标签和占位，减少授权与状态筛选区仍残留硬编码中文的问题。
- 已把真实 Provider Smoke 录入区的分类与结果下拉补成“中文解释 + 技术值”双语表达，覆盖“自动分类、仅授权验证、仅浏览验证、快速上传成功、二进制上传成功、部分阻塞、失败、成功 / 失败”等选项，让中文模式下不再只暴露原始 `auth_only / fast_upload_success / success / failure` 值。
- 已继续把任务向导、任务页和状态页里多组高频下拉枚举补成“中文解释 + 技术值”双语表达，覆盖“风控档位、执行模式、源端删除策略、冲突策略、重试分类、重试状态、目录状态、自动补传模式、自动补传策略、失败类型、执行状态”等首屏可见选项，减少中文模式下仍直接暴露 `leaf_first_lazy / retryable / runnable_now / fast_upload` 这类内部枚举值的问题。
- 已把任务页与状态页中共用的状态筛选项统一成一致的双语表达，例如“待处理（pending）、执行中（running）、阻塞中（blocked）、已完成（completed）、可立即重试（retryable）、已耗尽（exhausted）”，降低同一状态在不同页面上表述不一致的理解成本。
- 已继续把 `Provider Smoke` 记录区和验收矩阵区的高频交互提示接入双语词典，覆盖“按结果筛选 smoke 记录、按协议组筛选 smoke 记录、按验收状态筛选矩阵、打开 smoke 样本并回填表单、按验收缺口预填 smoke 动作、按验收矩阵预填 smoke 表单、按真实样本预填账号默认风控、清空 smoke 记录筛选”等常用反馈，减少页面已双语但操作反馈仍硬编码中文的割裂感。
- 已为 `Provider Smoke` 验收矩阵筛选提示补上“英文模式用英文标签、中文模式保留技术筛选值”的兼容策略，既保证普通用户读得懂，又避免现有 smoke 与静态断言依赖的关键技术锚点被完全抽走。
- 已把授权引导与目录浏览器第二批高频交互提示接入双语词典，覆盖“发现结果未命中、发现结果回填成功、打开 OpenList / Alist 登录页、加载 Provider 能力详情成功/失败、风控覆盖同步/清空、源/目标目录刷新、返回上级、打开目录、面包屑跳转”等反馈，减少授权和目录选择流程中仍残留硬编码中文 toast 的情况。
- 已把服务启动说明补齐为更完整的小白提示：启动日志除了本地访问地址和默认密码提醒外，新增了局域网访问、Docker / NAS 端口映射访问、桌面模式窗口自动打开与退出联动清理说明，并补上 `internal/app` 与 `internal/desktop` 针对性测试，避免三期要求只停留在 README 而没有实际输出。
- 已把桌面模式打开失败时的兜底说明补成更明确的中文提示：现在会区分“未找到 Chrome / Edge 独立窗口浏览器”与“一般独立窗口打开失败”两类场景，并统一提示可手动访问本地控制台地址；同时补上 Windows 本地安装 Edge 的候选路径与对应测试，减少桌面包在用户机器上明明装了 Edge 却没被识别的情况。
- 已把桌面模式启动阶段补成更直白的用户提示：本地服务就绪后会直接输出当前控制台地址，并明确说明当前是“Chrome / Edge 独立窗口模式”还是“系统浏览器兜底模式”；如果窗口或浏览器没有自动弹出，也会提示手动访问本地地址，减少桌面包启动后“看起来没反应”带来的困惑。
- 已继续补齐桌面模式的退出提示语义：独立窗口模式下，关闭窗口时会明确提示“正在退出本地服务”；系统浏览器兜底模式下，会直接提醒“关闭浏览器标签页不会自动退出服务，需要关闭终端或按 Ctrl+C”，减少桌面包在兜底模式下让用户误以为服务已经结束的情况。
- 已把 README 里的桌面模式说明同步到最新行为：新增“启动后会看到哪些提示”、独立窗口模式与系统浏览器兜底模式的区别，以及 Windows / Linux / macOS 下分别如何退出服务，避免代码和文档对桌面模式退出方式描述不一致。
- 已把 README 里的 GitHub 打包说明同步到仓库现状：去掉过时的 `docker-package` 表述，统一说明当前由 `release-package` 工作流负责服务端包、桌面包和 Docker 归档发布，避免用户按旧名字去 GitHub Actions 里找不存在的工作流。
- 已把 Release 正文描述补成更面向小白的中文分组说明：现在会直接提示“普通电脑用户优先下桌面包、服务器/NAS 用户下服务端包、Docker 用户下镜像归档包”，并补上 `SHA256SUMS.txt` 的用途说明，减少用户打开发布页后还要自己猜每类资产适合谁。
- 已把 Release 工作流补上产物白名单校验：发布前会显式校验服务端包、桌面包、Docker 归档和 `SHA256SUMS.txt` 是否与约定名单完全一致，并额外拦截 `cloud-clipboard / clipboard` 这类外部项目命名，减少 GitHub 打包阶段再次混入错误资产的风险。
- 已把 Docker 归档标签校验补进 Release 工作流：GitHub 打包时会先加载生成出来的 Docker tar，再显式检查 `cloudpan-sync-go:release` 和 `cloudpan-sync-go:latest` 两个标签是否都存在，减少发布后导入镜像只能拿到单个标签、还要手动补 tag 的情况。
- 已继续收紧 Docker 归档校验：GitHub 打包时在确认 `release/latest` 双标签存在后，还会检查仓库名只能是 `cloudpan-sync-go`，避免把其它镜像仓库名一起混进发布归档。
- 已继续把授权页与任务向导里一批高频前置提示接入双语词典，覆盖 `Extra JSON` 解析失败、任务风控覆盖 JSON 解析失败、推荐执行/风控采用、计划预览生成、删除记录拦截、任务创建，以及“请先选择任务 / 缺少任务标识 / 任务操作执行中”等提示，减少英文模式下这些前置反馈仍固定中文的问题。
- 已继续把授权页和任务向导里的第二批安全交互提示接入双语词典，覆盖“未找到要编辑的授权档案、已载入授权档案编辑表单、授权校验完成、授权档案已删除、按当前任务重建向导参数、任务创建参数已复制到剪贴板、复制失败”等反馈，减少英文模式下授权维护和任务回填操作仍夹杂固定中文的问题。
- 已继续把任务向导范围重建相关提示接入双语词典，覆盖“按路径重建向导范围、按筛选标签重建向导范围、当前向导参数无法解析、缺少可重建路径、当前没有可重建的删除记录、当前状态样本没有可用任务”等反馈，减少删除记录回填和路径级重建操作在英文模式下仍固定中文的问题。
- 已继续把任务处理建议卡和页面跳转类反馈接入双语词典，覆盖“已定位到当前授权档案、按指定自动补传模式收敛候选、已打开状态矩阵、已按当前任务预填任务向导参数”等提示，减少从任务详情跳转到授权页、状态页、任务向导时这些反馈仍固定中文的问题。
- 已把服务端包和桌面包的压缩内容校验补进 Release 工作流：GitHub 打包后会直接检查 zip / tar.gz 内部是否只包含约定的本项目二进制，例如 `cloudpan-sync` 或 `cloudpan-sync-desktop`，减少归档里混入其它文件、旧产物或错误项目文件的风险。
- 已把 `SHA256SUMS.txt` 覆盖校验补进 Release 工作流：GitHub 打包后会显式检查校验文件里的条目是否与所有应发布资产一一对应，避免出现校验文件漏掉桌面包、Docker 包或平台包的情况。
- 已继续把任务页与状态页目录树/待补传树相关的一批安全页内反馈接入双语词典，覆盖按扫描轨迹或选定根目录定位树面板，以及清空任务/状态筛选后的提示，减少英文模式下树筛选操作仍固定中文的问题。
- 已继续把任务页目录树与重试队列的一批低风险页内反馈接入双语词典，覆盖复制可见路径、复制子树路径、按父级或指定路径收敛树面板、按当前重试项定位待补传树、按 blocked action 收敛重试队列，以及打开 blocked 样本任务等提示，减少英文模式下这些局部交互反馈仍固定中文的问题。
- 已继续把状态页报告区与辅助操作的一批低风险页内反馈接入双语词典，覆盖任务列表刷新、状态矩阵刷新、smoke 记录保存、验收报告刷新/保存/下载/切换，以及 smoke Markdown 切换/下载等提示，减少英文模式下这些辅助操作反馈仍固定中文的问题。
- 已继续把状态页后台补传候选池的一批低风险页内反馈接入双语词典，覆盖按决策预演/执行、按决策状态或 lane 收敛候选、按模式/协议族/执行状态/主失败类型/阻塞动作收敛候选，以及采用建议预算等提示，减少英文模式下这些候选池辅助反馈仍固定中文的问题。
- 已继续把任务主流程里剩余的一条低风险动作结果提示接入双语词典，覆盖运行、暂停、继续、重试等任务动作执行完成后的反馈，减少英文模式下主流程完成提示仍直接拼接中文动作名的问题。
- 已继续把任务向导、验收矩阵与路径复制操作里一批低风险兜底提示接入双语词典，覆盖“当前状态样本没有可用任务、当前筛选结果里没有可用路径、当前路径无效、未找到验收矩阵项、当前没有可打开的任务样本、复制目录/待补传路径失败”等反馈，减少英文模式下这些边缘错误提示仍固定中文的问题。
- 已继续把授权引导地址校验、后台补传决策兜底、恢复路径缺失，以及 smoke Markdown 加载/下载失败等剩余低风险异常提示接入双语词典，并补齐一处验收矩阵提示的遗漏接线，减少英文模式下这些边缘报错仍固定中文的问题。
- 已继续把后台补传预演/执行/子树执行结果提示，以及删除记录重建场景里的状态样本兜底提示接入双语词典，减少英文模式下这些恢复链路反馈仍固定中文的问题。
- 已继续把状态页协议族覆盖、最近结果、最近 probe 和持久化报告记录的空状态提示接入双语词典，减少英文模式下这些只读展示区仍固定中文的问题。
- 已继续把后台补传按筛选执行后的结果提示接入双语词典，补齐后台补传执行类反馈的最后一条低风险硬编码结果文案。
- 已继续把 Provider 能力详情默认提示、授权档案空状态和 smoke Markdown 预览空状态接入双语词典，减少英文模式下这些面板空态仍固定中文的问题。
- 已继续把后台补传候选池的空状态、摘要标题和等待态说明接入双语词典，减少英文模式下状态页候选池展示区仍固定中文的问题。
- 已继续把后台补传等待态建议、决策兜底说明和任务列表空状态接入双语词典，减少英文模式下这些深层只读说明仍固定中文的问题。
- 已继续把授权引导发现区剩余一批低风险动态文案接入双语词典，覆盖“无可见存储空态、可见存储摘要、存储选择按钮、仅展示前 12 项提示、选择可见存储引导、地址未填写校验”等提示，减少 OpenList / Alist 引导区在英文模式下仍夹杂固定中文的问题。
- 已继续把 Provider 能力详情与任务风控解释卡的一批只读标题接入双语词典，覆盖“能力摘要、授权方式、冲突策略、兜底策略、默认风控模板、风控链路、基础模板、校准结果、账号默认来源、来源类型 / 偏向、账号默认字段、任务覆盖字段、恢复预算理由”等说明，减少英文模式下这些详情卡仍固定中文的问题。
- 已继续把真实 Provider Smoke 草稿说明和 Markdown 加载失败提示接入双语词典，覆盖“协议组、建议 provider、验收状态、缺口、动作、建议、本次目标、边界样本补充提示、smoke Markdown 加载失败”等辅助说明，减少状态页 smoke 补样流程在英文模式下仍固定中文的问题。
- 已补齐 `internal/app` 静态断言与主线回归验证，本轮确认 `node --check web/static/app.js` 与 `go test ./internal/app -run ""TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline"" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把风控链路解释卡和网盘源默认风控详情卡的一批深层只读说明接入双语词典，覆盖“网盘源基线、网盘源校准后、账号默认注入、任务覆盖、最终生效、自动补传时段、恢复预算、预算建议、校准依据、风险特征、时间窗来源/建议、校准覆盖/完成/缺失、已覆盖字段、校准样本建议、推荐说明”等说明，减少英文模式下这些深层说明卡仍固定中文的问题。
- 已继续把目标授权档案洞察卡的说明提示接入双语词典，覆盖“可直接写入本次任务覆盖”“授权档案内置账号默认风控”“未配置，使用网盘源默认模板”等来源说明，减少英文模式下账号默认风控来源仍夹杂固定中文的问题。
- 已同步兼容 `internal/app` 静态断言依赖的中文锚点，补回“风险特征”“校准完成”等展示锚点，避免三期双语收口与现有静态验收互相打架。
- 已继续把状态页真实样本矩阵与上传断点续传摘要的一批残留展示文案接入双语词典，覆盖“补 1 条真实上传成功样本、补 1 条真实任务覆盖样本、补上传 smoke、补任务覆盖样本、补 smoke 成功样本、按缺口预填动作、边界样本 smoke、协议族覆盖空态、上传断点续传样本上下文”等提示，减少状态页样本补齐和恢复摘要仍夹杂固定中文的问题。
- 已继续把状态页真实样本验收矩阵里的检查项、缺口和状态词接入双语词典，覆盖“上传/覆盖/异常/代表 检查项、异常/代表样本缺口、已补齐、已就绪、进行中、待补齐、已完成”等提示，并保留静态锚点注释兼容 `internal/app` 断言，减少验收矩阵仍直接暴露 ready/pending/complete/internal gap 文本的问题。
- 已继续把状态页“最近结果 / 最近 Probe”表格的空态和表头接入双语词典，覆盖状态、模式、执行模式、重试模式、重试范围、重试路径、源端删除策略、风险命中、载荷、创建时间等列，并保留静态表头锚点注释兼容 `internal/app` 断言，减少状态页列表区仍直接依赖固定中文表头的问题。

- 已继续把任务页重试队列的剩余英文详情改成词典驱动，覆盖“尝试/上限/剩余、冷却、可重试时间、根目录、原因、下一步、续传断点”等说明，减少中文界面下这块仍直接暴露英文调试串的问题。
- 已继续把 Provider Smoke 记录详情区接入双语词典，覆盖模板/类型/完整度、复用建议、回归入口、代表标签、自动补传关注点、操作和创建时间，并保留现有按钮与数据结构不变，减少状态页记录详情仍中英混排的问题。
- 已继续把后台补传最近结果、决策预算提示、决策详情标签与候选池聚合统计的一批高频展示串改成词典驱动，覆盖 matched/recovered 结果摘要、current -> suggest 预算提示、provider/profile/strategy/mode/state 说明，以及 runnable / wait cooldown / wait auth 等候选统计，减少状态页自动补传区仍直接暴露英文运营字段的问题。
- 已补一条静态测试兼容锚点注释，保留 `internal/app/web_test.go` 依赖的 `current -> suggest` 预算原文，不回退真实中文展示，避免三期双语收口再次和现有静态断言互相冲突。
- 已继续补齐 Provider Smoke 详情区的静态测试兼容锚点，保留 `template:`、`reuse:`、`regression entry:`、`createdAt:` 等旧关键字供 `internal/app/web_test.go` 断言使用，不回退真实中文展示，避免三期双语收口与既有静态验收再次冲突。
- 已重新完成本轮主线回归验证，确认 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把后台补传预演、执行、子树执行和按筛选执行的结果提示接入双语词典，覆盖 matched/recovered、预算占用、阻塞与路径数量等摘要，减少状态页自动补传执行反馈仍夹杂英文预算字段的问题。
- 已继续把 Provider Smoke 草稿标题、后台补传候选池摘要标题，以及候选行里的 `queue / ready / cooldown / profiles / lanes / tasks` 这批高频统计串接入双语词典，减少状态页补样和候选池概览仍直接暴露英文运营标签的问题。
- 已再次完成本轮主线回归验证，确认上述状态页展示收口后 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把重试范围来源标签、Smoke Matrix 来源展示、真实样本风控模板标题，以及自动补传候选池空状态接入双语词典，减少状态页和账号默认风控回填场景里仍直接暴露固定中文硬编码的问题。
- 已顺手把 `renderProviderRiskTemplateDetail` 的默认标题参数改成词典兜底，避免翻译区之外还残留固定中文默认标题，继续收紧三期展示层的硬编码范围。
- 已继续把重试范围显示标签、成功/失败结果标签，以及后台补传路径校验报错接入双语词典，覆盖“目录子集 / 待补传子集 / 重试队列子集”“成功（success）/ 失败（failure）”“缺少可恢复路径”等提示，减少任务向导和状态页恢复链路里仍直接暴露固定中文硬编码的问题。
- 已再次完成本轮主线回归验证，确认上述辅助提示词典化后 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把授权引导说明卡的“当前网盘源 / 当前授权入口 / OpenList 优先 / Alist 兜底 / 手动高级模式”这批长摘要接入双语词典，减少授权主流程里仍固定中文说明的大段硬编码。
- 已继续把授权引导发现区的剩余摘要和兜底文案接入双语词典，覆盖“其余 N 项未展开”“发现结果默认提示 HTML”“未命名存储 / 未知驱动 / 状态未知”等内容，减少英文模式下 OpenList / Alist 发现结果仍夹杂中文回退文案的问题。
- 已完成本轮回归验证，确认上述授权引导词典化后 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把中文模式里一批仍显眼夹英文的展示词收口为中文优先表达，覆盖“支持验证与删除”“真实网盘样本记录”“样本文档”等文案，并保留 `Smoke Matrix` 等测试依赖锚点不回退主流程。
- 已再次完成本轮主线回归验证，确认上述中文化收口后 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把任务页和状态页一批长说明收口成“中文解释 + 必要技术词”格式，覆盖重试队列说明、自动补传通道提示、上传断点续跑提示、阻塞处理建议，以及网盘样本筛选占位提示，减少普通用户首屏仍要自己翻译 lane / provider / protocolGroup 等内部术语的问题。
- 已再次完成本轮主线回归验证，确认上述说明文案收口后 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。
- 已继续把状态页首屏几处仍显眼的英文术语收口为中文优先表达，覆盖“最近探测 / 快照”“阻塞汇总项”“只看该通道”“网盘源默认风控校准”“待补齐（待补真实网盘样本）”“样本记录”等文案，减少中文界面下 probe / lane / provider / smoke 等词在首屏和验收矩阵里继续裸露的问题。
- 已继续把状态页样本记录区一批高频 `smoke` 展示词收口成中文优先表达，覆盖“当前没有样本记录”“显示全部/当前显示样本记录”“打开样本记录”“预填样本表单”“最近样本时间”等文案，并同步更新相关 smoke/web 断言，减少中文界面下样本区仍持续暴露 `smoke` 混合提示的问题。
- 已继续把状态页样本补齐流程里的保存提示、筛选提示、补样动作和样本文档空态收口成中文优先表达，覆盖“网盘样本记录已保存”“已按标签收敛样本记录”“已按验收缺口预填样本动作”“补成功样本”“请选择一条样本记录查看样本文档”等文案，并同步更新相关 smoke/web 断言，减少中文界面下样本补齐链路仍持续暴露 `smoke / Markdown / lane` 混合提示的问题。
- 已继续把状态页首屏和真实样本矩阵里几处最显眼的混合词收口成中文优先表达，覆盖“网盘源状态矩阵”“下载样本文档”“协议族：aliyun_123_open”“样本记录”“最近样本 / 覆盖观察”等文案，并同步更新 smoke 断言，减少中文界面首屏和样本矩阵仍持续暴露 `Provider / Markdown / smoke` 混合提示的问题。
- 已继续把任务下一步空态、网盘源级真实样本验收空态收口为中文优先表达，覆盖“当前任务没有需要人工处理的阻塞动作（blocked）”“暂无网盘源级验收数据（providerSmokeProviders）”等提示，减少中文模式下仍直接暴露 `blocked / providerSmokeProviders` 字段名的问题。

- 已继续把任务处理建议里两处仍含英文括注的引导提示收口为中文优先表达，去掉 `upload checkpoint auto-resume 通道` 重复词，并清理 `阻塞聚合看板（blocked summary）` 末尾的英文括注，减少中文模式下重复词与冗余英文同时出现的问题。
- 已继续把任务处理建议里 `retry_queue_auto_retry 通道` 这条提示中重复的 `通道` 词去掉，与 `upload checkpoint auto-resume` 同步保持中文先行、英文括注收尾的写法，减少中文模式下两条接管引导出现重复用词的问题。
## 正在推进

- 继续清理 `web/static/app.js` 中仍暴露给用户的中英混合提示。
- 继续同步 `internal/app/web_test.go` 与 `internal/app/ui_smoke_test.go` 的文案断言。
- 按三期计划继续收口普通用户主流程可理解性，而不是继续堆叠底层能力展示。
- 继续把目录浏览器从“基础可用”推进到更完整的可视化选择器，例如更完整的创建结果反馈和更完整的 smoke 覆盖。
- 继续把双语覆盖范围从当前已完成的首屏、导航、登录区、授权区、任务向导，以及任务页 / 状态页前十一批高频区域，扩展到更多状态诊断长文案和剩余深层动态说明。

## 尚未开始或未完成

- OpenList 优先、Alist 兜底的授权引导流程已补“连通性 + 可见存储发现 + 存储结果回填”，后续继续接入更完整的真实账号字段映射和目录拉取能力。
- 源目录到目标目录的完整可视化选择器细化体验，例如多选/收藏和更广泛 smoke 验证。
- 原生桌面客户端入口和 Release 桌面包矩阵已补骨架，后续继续升级成真正的 WebView / Wails 窗口与更完整打包链路。
- 完整 `zh-CN / en-US` 双语字典对全页面控件、提示、空状态和运行文案的覆盖仍未完成，目前已完成首屏、导航、登录区、授权区、任务向导，以及任务页 / 状态页前四批高频入口。

## 本批次说明

- 本次记录只写已实际落地的项目，不把尚未交付的计划项提前记为完成。
- 已继续把状态页和任务处理建议里一批仍显眼的混合术语收口成中文优先表达，覆盖“retry_queue_auto_retry 通道”“upload checkpoint 自动续跑通道”“通道预算”“网盘源预算”“暂无探测证据”“待补真实网盘样本”等文案，减少中文模式下仍持续暴露 `lane / provider / probe` 混合提示的问题。

- 已把任务处理建议里的 `auto_recover_takeover_step_1` 引导文案收口为更亲和的中文表达，去掉直接暴露调度器术语 `tick` 的提法，统一同步 zh-CN 字典与渲染时 fallback，减少中文模式下还需要用户理解后台调度细节的负担。
- 已继续把任务处理建议卡的 fallback 默认值与 zh-CN 字典本体对齐，覆盖『更新 token / cookie 后先执行授权验证』『回到任务详情页，再执行继续或重试』『重新重试』『确认这类阻塞项已经收敛，再执行重试』『确认后再回到任务详情执行重试』『当前任务已经达到重试上限（retryLimit），继续原样重试也不会再推进』等表达，减少中文界面在词典缓存未命中时仍可能直接显示 `Validate / Resume / Retry / blocked` 等英文动作词的问题。
- 已继续把任务处理建议卡和状态页里 5 条 fallback 默认值对齐到 zh-CN 字典：把 Provider 会话 / provider 默认模板校准 / provider 列表 / 本地 fallback 路径 / provider 返回体 这种英文术语换成「网盘会话 / 网盘源默认模板校准 / 网盘源列表 / 本地兜底路径 / 网盘返回体」，让 fallback 路径在缓存命中时也保持中文优先。

- 已继续把任务详情重试范围摘要里 `pending_only (N items)` / `full_task` 这两条 fallback 默认值对齐到 zh-CN 字典本体的`仅待处理项 ({count} 项)` / `整个任务`，减少中文模式下任务详情顶部仍直接暴露英文运营字段的问题。


- 已把状态矩阵决策预算提示里第 7794 行的 fallback 默认值「lane 预算」改成「通道预算（lane）」，跟邻居「模式预算 / 协议组预算 / 网盘源预算 / 授权档案预算」保持一致的中文优先表达，避免单条混排英文运营词。zh-CN 字典里 status.auto_recover_budget_hint_label_* 系列尚未单独定义，目前 fallback 就是实际渲染文案。

- 已修复一批双语字典覆盖缺口（主线二）：用脚本对比 `zh-CN / en-US` 两套字典后发现 18 个 key 只在中文侧定义、英文模式会漏翻成中文，包括 10 个 `wizard.flash_*`（Extra JSON 解析失败、授权档案编辑/校验/删除、按任务重建向导、复制创建参数等）和 8 个 `tasks.flash_*`（目录树/待补传树按扫描轨迹或选定根目录定位、清空目录树/待补传筛选）。已为这些 key 补齐英文译文，让英文模式不再夹中文提示。
- 已修复 2 处 en-US 字典里仍残留中文的值（`tasks.result_count_compact`、`status.smoke_draft_title_status`），改成英文模板，避免英文模式下结果计数和样本标题仍显示中文。
- 已修复一处命名空间错位：`flash_auto_recover_filters_cleared` 运行时按 `status.` 前缀调用，但 zh-CN 只在 `tasks` 命名空间定义、en-US 只在 `status` 命名空间定义，两端都靠 fallback 兜底。已统一把 zh-CN 该 key 移到 `status` 命名空间并删掉 `tasks` 下的死副本，让中英两侧都能真正命中字典。
- 已把若干内联 fallback 与字典本体对齐，消除“缓存命中显示一种文案、缓存未命中显示另一种文案”的漂移，覆盖「provider 会话→网盘会话」「恢复等待 - Auth 刷新→恢复等待 - 刷新授权」「重试当前 root→重试当前根目录」「重试 checkpoint 数→重试断点数（checkpoint）」「全部 provider→全部网盘源」等。
- 已为 `status.auto_recover_budget_hint_label_*` 系列 5 个预算标签补齐 zh-CN / en-US 双语定义，之前只有内联 fallback、英文模式会显示中文预算标签。
- 上述双语收口后确认 `node --check web/static/app.js` 通过，`go test ./internal/app -count=1` 全量通过，未留下额外临时文件或后台进程残留。

- 已把 `action_manual_intervention_required` 与 `guide_manual_intervention_title` 里残留的「provider 会话」对齐为「网盘会话」，与已收口的 `wait_provider_session` 保持一致，并同步更新 `internal/app/ui_smoke_test.go` 里依赖的「修复 provider 会话缺口 / 修复 provider 会话后继续」锚点，让任务处理建议卡里这类引导在中文模式下不再单独暴露英文术语。
- 已把恢复等待提示 `wait_auth_refresh` / `wait_local_restore` 的内联 fallback 从「恢复等待 - Auth 刷新 / 恢复等待 - 本地恢复」对齐到 zh-CN 字典本体的「恢复等待 - 刷新授权 / 恢复等待 - 补回本地文件」，消除缓存未命中时 fallback 与字典两套文案不一致的问题。
- 已把目录树面板里 `rebuild_from_root / retry_current_root / auto_recover_current_root / focus_root` 这组按钮文案从直接暴露英文 `root` 改成「根目录」，覆盖 zh-CN 字典和内联 fallback，与界面其它「根目录」表达保持一致。
- 已把 `status.blocked_empty`、`status.all_providers`、`status.provider_smoke_acceptance_empty`、`tasks.retry_checkpoint_count` 这几条的内联 fallback 对齐到 zh-CN 字典本体（阻塞汇总项 / 全部网盘源 / 网盘源级验收数据 / 重试断点数），消除 fallback 仍残留 `blocked / provider / checkpoint` 而字典已收口的不一致。
- 已为状态矩阵决策预算提示补齐 `status.auto_recover_budget_hint_label_*`（mode / lane / group / provider / profile）五条 zh-CN 与 en-US 字典项，让英文模式下这组预算标签不再回退中文，同时保留中文模式既有「通道预算（lane）」等表达。
- 已修复执行模式 / 风控档位 / 授权方式三个标签渲染函数（`renderExecutionModeLabel / renderRiskModeLabel / renderAuthModeLabel`）一直硬编码中文、英文模式仍显示中文的问题：这三个函数原本直接返回固定中文 map，现在改走 `t()` 词典，命中 `wizard.execution_mode_* / wizard.risk_mode_* / wizard.auth_mode_*`。其中 `auth_mode_*` 四条 key 之前两套字典都没有，已补齐 zh-CN / en-US 双语；`risk_mode_balanced/safe` 的字典 zh 文案与实际渲染并测试依赖的「平衡 / 稳妥」不一致（字典写的是「均衡 / 保守」），已统一对齐到「平衡 / 稳妥」。这三个标签函数被授权档案表格、Provider 卡、目标洞察卡、授权方式下拉等多处复用，切到英文后这些位置不再夹中文枚举说明。
- 上述标签函数词典化后确认 `node --check web/static/app.js` 通过、`go test ./internal/app -count=1` 全量通过，未留下额外临时文件或后台进程残留。

- 已把任务处理建议卡的「provider 会话」相关 fallback 与 zh-CN 字典对齐成「网盘会话」，覆盖 `action_manual_intervention_required`（修复网盘会话后继续）与 `guide_manual_intervention_title`（修复网盘会话缺口），跟早先已收口的 `wait_provider_session`（恢复等待 - 网盘会话）保持一致，并同步更新 `ui_smoke_test.go` 里依赖这两条文案的断言锚点，避免中文模式下同类术语一处「网盘会话」、一处「provider 会话」混排。
- 已把恢复等待提示里两条 fallback 与字典对齐：`wait_auth_refresh` 渲染 fallback 从「恢复等待 - Auth 刷新」改成「恢复等待 - 刷新授权」，`wait_local_restore` 从「恢复等待 - 本地恢复」改成「恢复等待 - 补回本地文件」，让缓存未命中时的 fallback 与字典本体一致，避免同一标签出现两套中文表述。
- 已把目录树/待补传树面板里直接暴露英文 `root` 的四个按钮 fallback 与字典统一收口成「根目录」表达，覆盖 `rebuild_from_root`（按当前根目录重建向导）、`retry_current_root`（重试当前根目录）、`auto_recover_current_root`（后台补传当前根目录）、`focus_root`（只看根目录），减少中文模式下树面板按钮仍裸露 `root` 的问题。
- 已完成本轮回归验证，确认 `node --check web/static/app.js` 与 `go test ./internal/app -run "TestHandleIndexServesHTML|TestRoutesServeAppJSIncludesRetryEvidenceLabels|TestConsoleUISmokeMainline" -count=1` 通过，且未留下额外临时文件或后台进程残留。